extends layout

block content
  h1 #{title}

  p #[strong Author: ]
    a(href=book.author.url) #{book.author.name}
  p #[strong isbn: ] #{book.isbn}
  p #[strong id: ] #{book._id}

  each val in meeting
      hr
      p #[strong meeting: ] #{val.place}

  else
      p This book is not used.


